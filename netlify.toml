[build]
  base    = "admin-panel"
  command = "npm install && npm run build"
  publish = "admin-panel/dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
